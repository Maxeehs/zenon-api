spring:
  # Désactive Liquibase et Flyway
  liquibase:
    enabled: false
  flyway:
    enabled: false

  # (Optionnel) utiliser H2 en mémoire pour vos tests
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
